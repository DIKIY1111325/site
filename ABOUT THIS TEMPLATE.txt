процесс авторизации будет учитывать, откуда пришел пользователь и перенаправлять его на нужную страницу после успешной авторизации.

You said:
посмотри как колярируется с нашим кодом это CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email TEXT UNIQUE NOT NULL,
  password_hash TEXT NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
CREATE TABLE subscriptions (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  plan TEXT NOT NULL,
  status TEXT DEFAULT 'active',
  start_date TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  end_date TIMESTAMP WITH TIME ZONE
);

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
-- Для таблицы users
ALTER TABLE users ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can manage their own data"
ON users
FOR ALL
USING (auth.uid() = id);



Код для получения подписки после авторизации (в login.js):
javascript
Copy
Edit
// Инициализация Supabase
const supabaseUrl = 'https://dsvaqphuagrnkjmthtet.supabase.co';
const supabaseKey = 'your-anon-public-api-key'; // Замените на ваш публичный ключ
const supabase = supabase.createClient(supabaseUrl, supabaseKey);

// Функция для обработки формы авторизации
document.getElementById("login-form").addEventListener("submit", async function (e) {
    e.preventDefault();

    const email = document.getElementById("email").value;
    const password = document.getElementById("password").value;

    try {
        const { data, error } = await supabase.auth.signInWithPassword({
            email,
            password,
        });

        if (error) {
            alert("Ошибка авторизации: " + error.message);
            return;
        }

        // Получение информации о подписке пользователя после авторизации
        const user = supabase.auth.user();  // Получаем информацию о текущем пользователе
        const { data: subscriptionData, error: subscriptionError } = await supabase
            .from('subscriptions')
            .select('plan, status')
            .eq('user_id', user.id)
            .single();  // Используем .single(), так как подписка должна быть уникальной для каждого пользователя

        if (subscriptionError) {
            console.error("Ошибка получения подписки: ", subscriptionError);
        } else {
            console.log("Информация о подписке:", subscriptionData);
        }

        // Получение информации о кнопке, с которой перешли на страницу авторизации
        const redirectPage = localStorage.getItem("redirectPage");

        // Перенаправление пользователя в зависимости от выбранной кнопки
        if (redirectPage === "vpn_quick") {
            window.location.href = "invite.html";
        } else if (redirectPage === "vpn_cheap") {
            window.location.href = "faq3_vpn_serv.html";
        } else if (redirectPage === "personal_vpn") {
            window.location.href = "faq4_vpn_serv.html";
        } else {
            window.location.href = "index.html"; // В случае ошибки или неопределенной страницы
        }
    } catch (err) {
        console.error(err);
        alert("Произошла ошибка при авторизации.");
    }
});
Важные моменты:
Получение подписки: После успешной авторизации мы извлекаем информацию о подписке пользователя с помощью запроса:

javascript
Copy
Edit
const { data: subscriptionData, error: subscriptionError } = await supabase
    .from('subscriptions')
    .select('plan, status')
    .eq('user_id', user.id)
    .single();
Мы используем метод .eq('user_id', user.id) для фильтрации подписки по ID пользователя, а .single() — чтобы ожидать только одну запись (так как каждый пользователь может иметь только одну подписку).

Редирект в зависимости от выбранной кнопки: Перенаправление остается таким же, как в предыдущем примере, с использованием localStorage.
